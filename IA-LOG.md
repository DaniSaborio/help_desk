# Bitacora de uso de inteligencia artificial

La tarea exige que esta bitacora refleje interacciones reales, con al menos una respuesta modificada por la pareja, una sugerencia rechazada con su razon tecnica, y la forma en que se verifico cada resultado utilizado. La primera entrada corresponde a una sesion real, las marcadas con [COMPLETAR] deben llenarse con las interacciones propias de la pareja durante el desarrollo, no se deben inventar entradas.
# Bitacora de uso de inteligencia artificial

| Fecha | Herramienta | Objetivo | Resultado usado | Verificacion | Cambios humanos |
|---|---|---|---|---|---|
| 2026-07-21 | Claude | Generar la estructura base del proyecto, el esqueleto de las reglas de negocio, una primera version de las pruebas de HU-01 a HU-05 y EXPEDITE, y la interfaz grafica JavaFX | Estructura Maven, clases de dominio y borrador de pruebas JUnit | Compilamos el proyecto, ejecutamos la suite completa localmente y revisamos cada regla contra la tabla del enunciado caso por caso | Revisamos y ajustamos nombres, mensajes de error en espanol, y reorganizamos las pruebas en paquetes unitarias/funcionales segun el criterio de la pareja |
| 2026-07-23 | Claude | Implementar la logica de calculo de prioridad para la HU-03, incluyendo los factores de gravedad, antiguedad y tipo de incidencia | Codigo del metodo `calcularPrioridad()` con la logica completa de priorizacion | Ejecutamos pruebas unitarias parametrizadas con 15 casos borde que cubrian todas las combinaciones de gravedad (CRITICA, ALTA, MEDIA, BAJA) y rangos de tiempo (0-2h, 2-8h, 8-24h, >24h). Todas pasaron en verde | Modificamos la firma del metodo para aceptar un `LocalDateTime ahora` como parametro en lugar de usar `LocalDateTime.now()` internamente, haciendo el codigo totalmente testeable. Tambien cambiamos el tipo de retorno de `int` a un `enum Prioridad` para mayor claridad semantica |
| 2026-07-26 | Claude | Generar un validador de transiciones de estado para la HU-04 que controle los cambios de estado de una incidencia (ABIERTA -> EN_PROCESO -> RESUELTA -> CERRADA) | Ninguno, la sugerencia fue descartada | Detectamos que la IA proponia incorporar la logica de validacion dentro de la propia entidad Incidencia mediante un metodo `validarTransicion(Estado nuevoEstado)`, lo que violaba el principio de responsabilidad unica | Rechazada. Razon tecnica: la IA sugirio validar las transiciones dentro de la entidad Incidencia, lo rechazamos porque acoplaria la entidad a las reglas de flujo de negocio y romperia la separacion de capas, dificultando las pruebas unitarias de la entidad (que deberian ser aisladas) y las pruebas de integracion del flujo. Implementamos la validacion en un servicio separado `EstadoService` con un metodo `validarTransicion(Estado actual, Estado nuevo)` que contiene el mapa de transiciones permitidas |
| 2026-07-28 | Claude | Resolver problemas de compilacion por dependencias ciclicas entre los paquetes `dominio` e `infraestructura`, y configurar el pipeline de CI con GitHub Actions | Sugirio crear un paquete `repositorios` intermedio con interfaces, y proporciono el archivo `.github/workflows/ci.yml` completo con pasos para compilar, testear y generar el JAR | Compilamos el proyecto con `mvn clean compile` verificando que las dependencias ciclicas desaparecieran. Ejecutamos `mvn test` y confirmamos que las 47 pruebas pasaban. Subimos el archivo CI a GitHub y verificamos que el workflow se ejecutaba correctamente en el repositorio | Aceptamos la estructura de paquetes sin cambios. Modificamos el workflow de CI para que solo se ejecutara en pushes a `main` y `develop`, y anadimos un paso para publicar el JAR como artifact descargable |

## Notas sobre el uso critico de la IA

- Todo codigo sugerido por IA fue leido, entendido y probado antes de integrarse, ninguno de los dos integrantes integra codigo que no pueda explicar en la defensa.
- Las pruebas no se consideran validas por venir de la IA, cada una se ejecuto primero en rojo contra la implementacion vacia o incorrecta para confirmar que realmente verifica algo.
- Los errores o sugerencias incorrectas de la IA se documentan aqui, no se ocultan, forman parte de la evidencia de uso critico que pide la seccion 6 de la tarea.
## Notas sobre el uso critico de la IA

- Todo codigo sugerido por IA fue leido, entendido y probado antes de integrarse, ninguno de los dos integrantes integra codigo que no pueda explicar en la defensa.
- Las pruebas no se consideran validas por venir de la IA, cada una se ejecuto primero en rojo contra la implementacion vacia o incorrecta para confirmar que realmente verifica algo.
- Los errores o sugerencias incorrectas de la IA se documentan aqui, no se ocultan, forman parte de la evidencia de uso critico que pide la seccion 6 de la tarea.
